diff --git a/droid/camera_utils/camera_readers/zed_camera.py b/droid/camera_utils/camera_readers/zed_camera.py
index 5d1d3de..7b34422 100644
--- a/droid/camera_utils/camera_readers/zed_camera.py
+++ b/droid/camera_utils/camera_readers/zed_camera.py
@@ -12,7 +12,7 @@ except ModuleNotFoundError:
     print("WARNING: You have not setup the ZED cameras, and currently cannot use them")
 
 
-def gather_zed_cameras():
+def gather_zed_cameras(camera_serials=None, wrist_camera_serial=None):
     all_zed_cameras = []
     try:
         cameras = sl.Camera.get_device_list()
@@ -20,7 +20,9 @@ def gather_zed_cameras():
         return []
 
     for cam in cameras:
-        cam = ZedCamera(cam)
+        if camera_serials is not None and str(cam.serial_number) not in camera_serials:
+            continue
+        cam = ZedCamera(cam, wrist_camera_serial)
         all_zed_cameras.append(cam)
 
     return all_zed_cameras
@@ -42,10 +44,10 @@ advanced_params = dict(
 
 
 class ZedCamera:
-    def __init__(self, camera):
+    def __init__(self, camera, wrist_camera_serial=None):
         # Save Parameters #
         self.serial_number = str(camera.serial_number)
-        self.is_hand_camera = self.serial_number == hand_camera_id
+        self.is_hand_camera = self.serial_number == (hand_camera_id if wrist_camera_serial is None else wrist_camera_serial)
         self.high_res_calibration = False
         self.current_mode = None
         self._current_params = None
diff --git a/droid/camera_utils/wrappers/multi_camera_wrapper.py b/droid/camera_utils/wrappers/multi_camera_wrapper.py
index 5393fe6..a55114c 100644
--- a/droid/camera_utils/wrappers/multi_camera_wrapper.py
+++ b/droid/camera_utils/wrappers/multi_camera_wrapper.py
@@ -8,14 +8,16 @@ from droid.camera_utils.info import get_camera_type
 
 
 class MultiCameraWrapper:
-    def __init__(self, camera_kwargs={}):
+    def __init__(self, camera_kwargs={}, camera_serials=None, wrist_camera_serial=None):
         # Open Cameras #
-        zed_cameras = gather_zed_cameras()
+        zed_cameras = gather_zed_cameras(camera_serials, wrist_camera_serial)
         self.camera_dict = {cam.serial_number: cam for cam in zed_cameras}
 
         # Set Correct Parameters #
         for cam_id in self.camera_dict.keys():
             cam_type = get_camera_type(cam_id)
+            if camera_serials is not None:
+                cam_type = "hand_camera" if cam_id == wrist_camera_serial else "static_camera"
             curr_cam_kwargs = camera_kwargs.get(cam_type, {})
             self.camera_dict[cam_id].set_reading_parameters(**curr_cam_kwargs)
 
diff --git a/droid/franka/launch_gripper.sh b/droid/franka/launch_gripper.sh
index 414dfd5..5f44d32 100755
--- a/droid/franka/launch_gripper.sh
+++ b/droid/franka/launch_gripper.sh
@@ -1,5 +1,5 @@
 source ~/anaconda3/etc/profile.d/conda.sh
 conda activate polymetis-local
-pkill -9 gripper
-chmod a+rw /dev/ttyUSB0
-launch_gripper.py gripper=robotiq_2f gripper.comport=/dev/ttyUSB0
+device="${1:-/dev/ttyUSB0}"
+chmod a+rw "$device"
+exec launch_gripper.py gripper=robotiq_2f "gripper.comport=$device" "port=${2:-50052}"
diff --git a/droid/franka/launch_robot.sh b/droid/franka/launch_robot.sh
index 6bcff9e..140b295 100755
--- a/droid/franka/launch_robot.sh
+++ b/droid/franka/launch_robot.sh
@@ -1,5 +1,3 @@
 source ~/anaconda3/etc/profile.d/conda.sh
 conda activate polymetis-local
-pkill -9 run_server
-pkill -9 franka_panda_cl
-launch_robot.py robot_client=franka_hardware
+exec launch_robot.py robot_client=franka_hardware "robot_client.executable_cfg.robot_ip=${1:-172.16.0.2}" "port=${2:-50051}"
diff --git a/droid/franka/robot.py b/droid/franka/robot.py
index 1985165..46cb727 100644
--- a/droid/franka/robot.py
+++ b/droid/franka/robot.py
@@ -1,5 +1,6 @@
 # ROBOT SPECIFIC IMPORTS
 import os
+import subprocess
 import time
 
 import grpc
@@ -8,7 +9,7 @@ import torch
 from polymetis import GripperInterface, RobotInterface
 
 from droid.misc.parameters import sudo_password
-from droid.misc.subprocess_utils import run_terminal_command, run_threaded_command
+from droid.misc.subprocess_utils import run_threaded_command
 
 # UTILITY SPECIFIC IMPORTS
 from droid.misc.transformations import add_poses, euler_to_quat, pose_diff, quat_to_euler
@@ -16,32 +17,45 @@ from droid.robot_ik.robot_ik_solver import RobotIKSolver
 
 
 class FrankaRobot:
-    def launch_controller(self):
-        try:
-            self.kill_controller()
-        except:
-            pass
+    def __init__(self, robot_ip="172.16.0.2", robot_port=50051, gripper_port=50052,
+                 gripper_device="/dev/ttyUSB0"):
+        self.robot_ip = robot_ip
+        self.robot_port = robot_port
+        self.gripper_port = gripper_port
+        self.gripper_device = gripper_device
+        self._controller_processes = []
 
+    def launch_controller(self):
+        self.kill_controller()
         dir_path = os.path.dirname(os.path.realpath(__file__))
-        self._robot_process = run_terminal_command(
-            "echo " + sudo_password + " | sudo -S " + "bash " + dir_path + "/launch_robot.sh"
-        )
-        self._gripper_process = run_terminal_command(
-            "echo " + sudo_password + " | sudo -S " + "bash " + dir_path + "/launch_gripper.sh"
-        )
+        for script, args in (
+            ("launch_robot.sh", [self.robot_ip, str(self.robot_port)]),
+            ("launch_gripper.sh", [self.gripper_device, str(self.gripper_port)]),
+        ):
+            process = subprocess.Popen(
+                ["sudo", "-S", "bash", os.path.join(dir_path, script), *args],
+                stdin=subprocess.PIPE, text=True, start_new_session=True,
+            )
+            process.stdin.write(sudo_password + "\n")
+            process.stdin.close()
+            self._controller_processes.append(process)
         self._server_launched = True
         time.sleep(5)
 
     def launch_robot(self):
-        self._robot = RobotInterface(ip_address="localhost")
-        self._gripper = GripperInterface(ip_address="localhost")
+        self._robot = RobotInterface(ip_address="localhost", port=self.robot_port)
+        self._gripper = GripperInterface(ip_address="localhost", port=self.gripper_port)
         self._max_gripper_width = self._gripper.metadata.max_width
         self._ik_solver = RobotIKSolver()
         self._controller_not_loaded = False
 
     def kill_controller(self):
-        self._robot_process.kill()
-        self._gripper_process.kill()
+        for process in self._controller_processes:
+            if process.poll() is None:
+                subprocess.run(["sudo", "-S", "kill", "-TERM", "--", f"-{process.pid}"],
+                               input=sudo_password + "\n", text=True, check=True)
+                process.wait(timeout=10)
+        self._controller_processes.clear()
 
     def update_command(self, command, action_space="cartesian_velocity", gripper_action_space=None, blocking=False):
         action_dict = self.create_action_dict(command, action_space=action_space, gripper_action_space=gripper_action_space)
diff --git a/droid/misc/server_interface.py b/droid/misc/server_interface.py
index d13152e..15e362d 100644
--- a/droid/misc/server_interface.py
+++ b/droid/misc/server_interface.py
@@ -21,17 +21,20 @@ def attempt_n_times(function_list, max_attempts, sleep_time=0.1):
 
 
 class ServerInterface:
-    def __init__(self, ip_address="127.0.0.1", launch=True):
+    def __init__(self, ip_address="127.0.0.1", launch=True, port=4242):
         self.ip_address = ip_address
+        self.port = port
         self.establish_connection()
 
         if launch:
             func_list = [self.launch_controller, self.launch_robot]
             attempt_n_times(func_list, max_attempts=2)
+        else:
+            self.launch_robot()
 
     def establish_connection(self):
         self.server = zerorpc.Client(heartbeat=20)
-        self.server.connect("tcp://" + self.ip_address + ":4242")
+        self.server.connect(f"tcp://{self.ip_address}:{self.port}")
 
     def launch_controller(self):
         self.server.launch_controller()
diff --git a/droid/robot_env.py b/droid/robot_env.py
index 4f223ed..64e1015 100644
--- a/droid/robot_env.py
+++ b/droid/robot_env.py
@@ -23,6 +23,11 @@ class RobotEnv(gym.Env):
         reset_joints=None,
         randomize_low=None,
         randomize_high=None,
+        robot_server_ip=nuc_ip,
+        robot_server_port=4242,
+        launch_controller=True,
+        camera_serials=None,
+        wrist_camera_serial=hand_camera_id,
     ):
         # Initialize Gym Environment
         super().__init__()
@@ -47,17 +52,20 @@ class RobotEnv(gym.Env):
         self.DoF = 7 if ('cartesian' in action_space) else 8
         self.control_hz = 10
 
-        if nuc_ip is None:
+        self.hand_camera_id = wrist_camera_serial
+        if robot_server_ip is None:
             from franka.robot import FrankaRobot
 
             self._robot = FrankaRobot()
         else:
-            self._robot = ServerInterface(ip_address=nuc_ip)
+            self._robot = ServerInterface(ip_address=robot_server_ip, port=robot_server_port, launch=launch_controller)
 
         # Create Cameras
-        self.camera_reader = MultiCameraWrapper(camera_kwargs)
+        self.camera_reader = MultiCameraWrapper(camera_kwargs, camera_serials, wrist_camera_serial)
         self.calibration_dict = load_calibration_info()
-        self.camera_type_dict = camera_type_dict
+        self.camera_type_dict = (camera_type_dict if camera_serials is None else
+                                 {str(serial): 0 if str(serial) == wrist_camera_serial else 1
+                                  for serial in camera_serials})
 
         # # Reset Robot
         # if do_reset:
@@ -124,7 +132,7 @@ class RobotEnv(gym.Env):
         # Adjust gripper camere by current pose
         extrinsics = deepcopy(self.calibration_dict)
         for cam_id in self.calibration_dict:
-            if hand_camera_id not in cam_id:
+            if self.hand_camera_id not in cam_id:
                 continue
             gripper_pose = state_dict["cartesian_position"]
             extrinsics[cam_id + "_gripper_offset"] = extrinsics[cam_id]
diff --git a/scripts/server/run_server.py b/scripts/server/run_server.py
index a8df802..63316e4 100644
--- a/scripts/server/run_server.py
+++ b/scripts/server/run_server.py
@@ -1,9 +1,20 @@
 import zerorpc
+import argparse
 
 from droid.franka.robot import FrankaRobot
 
 if __name__ == "__main__":
-    robot_client = FrankaRobot()
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--port", type=int, default=4242)
+    parser.add_argument("--robot-ip", default="172.16.0.2")
+    parser.add_argument("--robot-port", type=int, default=50051)
+    parser.add_argument("--gripper-port", type=int, default=50052)
+    parser.add_argument("--gripper-device", default="/dev/ttyUSB0")
+    args = parser.parse_args()
+    robot_client = FrankaRobot(args.robot_ip, args.robot_port, args.gripper_port, args.gripper_device)
     s = zerorpc.Server(robot_client)
-    s.bind("tcp://0.0.0.0:4242")
-    s.run()
+    s.bind(f"tcp://0.0.0.0:{args.port}")
+    try:
+        s.run()
+    finally:
+        robot_client.kill_controller()
